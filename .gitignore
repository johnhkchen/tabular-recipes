node_modules/
dist/
.astro/
# Rebuilt from recipes/**/*.cook by `npm run recipes`.
src/generated/
# Machine-local agent settings, regenerated by `lisa init`.
.claude/settings.local.json
# Lisa's own run state — journals of which agent ran what, and the terminal layout.
# The reviewable record of the work is in docs/active/work/, which is committed.
.lisa/
.lisa-layout.kdl
.DS_Store
